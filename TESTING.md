# Testing

Mactrix uses snapshot and layout tests to catch visual regressions and layout stability issues across the UI. Tests live in `MactrixLibrary/Tests/UITests/` and run via Swift Package Manager.

## Running Tests

```bash
cd MactrixLibrary
swift test --filter UITests
```

The full suite (~30 tests) runs in under a second.

## Test Structure

| File | Purpose |
|---|---|
| `SnapshotTestHelper.swift` | Shared helper for rendering SwiftUI views into `NSHostingView` and asserting snapshots |
| `PreviewSnapshotTests.swift` | Snapshot tests covering all MactrixLibrary UI views, grouped by area (timeline, sidebar, inspector, rooms, shared components) |
| `MessageEventBodyViewSnapshotTests.swift` | Targeted snapshot tests for the message event body in different states (reactions, read receipts, empty) |
| `MessageEventBodyViewLayoutTests.swift` | Layout stability tests — verifies view tree behaviour and guards against infinite layout loops |
| `__Snapshots__/` | Reference PNG images committed to the repository |

## Snapshot Tests

Snapshot tests render each view into an `NSHostingView`, capture a PNG, and compare it against a committed reference image. They use [swift-snapshot-testing](https://github.com/pointfreeco/swift-snapshot-testing) with a perceptual precision tolerance of 98% to account for minor rendering differences.

### First run on a new machine

On first run (or after deleting `__Snapshots__/`), all snapshot tests will fail and record new reference images. Run the tests a second time to verify they pass against the newly recorded references.

### Updating snapshots after intentional UI changes

If you've changed a view's appearance intentionally:

1. Delete the relevant snapshot file(s) from `__Snapshots__/`
2. Run `swift test --filter UITests` to record new references
3. Run again to confirm they pass
4. Commit the updated PNGs alongside your code changes

Alternatively, temporarily set the record mode in `SnapshotTestHelper.swift` to `.all` to re-record everything, then set it back to `.missing` before committing.

## Layout Tests

Layout tests verify structural behaviour rather than visual appearance:

- **`bottomRowCollapsesWithoutContent`** — Asserts that the message reaction/read receipt row adds height when content is present and collapses when empty.
- **`rapidReactionToggleDoesNotHang`** — Renders the message view 20 times with random reaction states under a time limit, catching infinite layout loop regressions caused by view tree instability in `NSHostingView`.

Both tests have a 1-minute timeout. If a layout loop occurs, the test hangs and fails on timeout.

## CI: Snapshot Generation and Verification

Snapshot images are pixel-sensitive to macOS version, display scale, and font rendering. To ensure consistency, reference snapshots should be generated by CI rather than committed from developer machines. There are three viable approaches, each with different trade-offs.

### Approach 1: CI commits snapshots back to the PR

CI records any missing snapshots and commits them back to the PR branch. This gives the best developer experience: visual changes are reviewable as PNG diffs in pull requests, and developers don't need to worry about local rendering differences.

#### How it works

1. A developer changes a view and deletes the corresponding snapshot file(s) from `__Snapshots__/`
2. They push the commit (with the deleted PNGs) to their PR branch
3. CI detects the missing snapshots, records them, and commits the new PNGs back to the branch
4. The developer (and reviewers) can inspect the updated snapshots in the PR diff
5. On subsequent pushes where snapshots haven't changed, CI simply verifies against the committed references

#### Workflow

Key points:

- **Pin the macOS runner version** (e.g. `macos-15`) — never use `macos-latest`, as runner updates would invalidate all snapshots
- **Pin the Xcode version** — font rendering and view layout can change between Xcode releases
- **Use `[ci skip]` in the commit message** to prevent the snapshot commit from triggering another CI run

```yaml
name: UI Snapshot Tests
on: [pull_request]

jobs:
  snapshot-tests:
    runs-on: macos-15
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_16.2.app

      - name: Run snapshot tests
        id: first_run
        continue-on-error: true
        run: cd MactrixLibrary && swift test --filter UITests

      - name: Record and commit missing snapshots
        if: steps.first_run.outcome == 'failure'
        run: |
          # Run again to record any missing snapshots
          cd MactrixLibrary && swift test --filter UITests || true
          cd ..

          # Commit new/updated snapshot PNGs if any were generated
          git add MactrixLibrary/Tests/UITests/__Snapshots__/
          if git diff --cached --quiet; then
            echo "No new snapshots to commit"
            exit 1  # Tests failed for a reason other than missing snapshots
          fi

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git commit -m "test: update snapshot references [ci skip]"
          git push

      - name: Verify snapshots
        if: steps.first_run.outcome == 'failure'
        run: cd MactrixLibrary && swift test --filter UITests
```

#### Behaviour

| Scenario | What happens |
|---|---|
| No view changes, snapshots up to date | First run passes, job succeeds |
| View changed, old snapshot deleted | First run fails (missing reference), records new PNG, commits it, verify step passes |
| View changed, snapshot not deleted | First run fails (pixel mismatch), no new files to commit, job fails — developer must delete the stale snapshot |
| New view added with new test | First run fails (missing reference), records new PNG, commits it, verify step passes |
| Layout test regression | First run fails, no snapshots to commit, job fails |

#### Trade-offs

- ✅ Visual diffs reviewable in PRs
- ✅ Consistent rendering — always generated on the same environment
- ✅ Developers don't need to manage snapshots locally
- ⚠️ CI pushes commits to PR branches, which some teams dislike
- ⚠️ macOS runners are slower and more expensive than Linux runners

### Approach 2: Cache-based (no snapshots in the repository)

CI records reference snapshots and stores them in the GitHub Actions cache. Snapshots are never committed to the repository.

#### Workflow

```yaml
name: UI Snapshot Tests
on: [pull_request]

jobs:
  snapshot-tests:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_16.2.app

      - name: Restore snapshot cache
        uses: actions/cache@v4
        with:
          path: MactrixLibrary/Tests/UITests/__Snapshots__
          key: snapshots-macos15-${{ hashFiles('MactrixLibrary/Tests/UITests/*.swift') }}

      - name: Run snapshot tests (record if missing)
        run: cd MactrixLibrary && swift test --filter UITests

      - name: Re-run to verify (if snapshots were just recorded)
        run: cd MactrixLibrary && swift test --filter UITests

      - name: Save snapshot cache
        uses: actions/cache/save@v4
        if: always()
        with:
          path: MactrixLibrary/Tests/UITests/__Snapshots__
          key: snapshots-macos15-${{ hashFiles('MactrixLibrary/Tests/UITests/*.swift') }}
```

The cache key is based on the test source files. When a test changes, the cache misses, snapshots re-record, and the second run verifies them.

#### Trade-offs

- ✅ No PNGs in the repository — keeps the repo small
- ✅ Simple setup, no commit-back logic
- ⚠️ Cannot review visual changes in PR diffs
- ⚠️ Cache can be evicted by GitHub (7-day inactivity or storage limits), causing a full re-record
- ⚠️ Harder to debug failures — no easy way to see what the snapshot looks like

### Approach 3: Artefact-based (snapshots as build artefacts)

CI always records fresh snapshots, runs a verification pass, and uploads the PNGs as build artefacts. Nothing is committed or cached.

#### Workflow

```yaml
name: UI Snapshot Tests
on: [pull_request]

jobs:
  snapshot-tests:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_16.2.app

      - name: Record snapshots
        run: cd MactrixLibrary && swift test --filter UITests || true

      - name: Verify snapshots
        run: cd MactrixLibrary && swift test --filter UITests

      - name: Upload snapshots on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: snapshot-failures
          path: |
            MactrixLibrary/Tests/UITests/__Snapshots__/
            /var/folders/**/MessageEventBodyViewSnapshotTests/
            /var/folders/**/PreviewSnapshotTests/
```

#### Trade-offs

- ✅ No PNGs in the repository
- ✅ No cache dependencies
- ✅ Failed snapshots downloadable as artefacts for inspection
- ⚠️ Cannot review visual changes in PR diffs
- ⚠️ No persistent baseline — every run records from scratch, so it only catches build failures, not regressions against a known-good state

### Recommendation

Each approach suits different team preferences. Approach 1 gives the best review experience, Approach 2 keeps the repository lean, and Approach 3 is the simplest to set up. The right choice depends on how important visual PR diffs and persistent baselines are to the team's workflow.

## Local Development

Developers can run snapshot tests locally for quick feedback, but should not commit locally-recorded PNGs to the repository (if using CI-based generation). If local snapshots differ from CI-generated ones due to macOS version differences, delete the local `__Snapshots__/` directory and let CI re-record them.

To preview snapshots locally without committing:

```bash
cd MactrixLibrary
rm -rf Tests/UITests/__Snapshots__
swift test --filter UITests  # Records references
swift test --filter UITests  # Verifies against them
git checkout -- Tests/UITests/__Snapshots__  # Restore CI-generated references
```

## Adding Tests for New Views

When adding a new view to `MactrixLibrary/Sources/UI/`:

1. Add a `#Preview` block to the view file with representative mock data
2. Add a corresponding snapshot test in `PreviewSnapshotTests.swift` under the appropriate section
3. Use `SnapshotTestHelper.assertViewSnapshot()` with a suitable width (600pt default, or narrower for sidebar/inspector views)
4. Push to your PR branch — CI will record the reference snapshot and commit it
5. Review the generated PNG in the PR diff

For views with complex layout behaviour (conditional content, dynamic sizing, AppKit embedding), consider adding a targeted layout test as well.

## Dependencies

The test target depends on:

- [swift-snapshot-testing](https://github.com/pointfreeco/swift-snapshot-testing) (v1.17+) — snapshot assertion library
- `UI` module — the views under test
- `Models` module (transitive) — mock types used in test fixtures

## Gaps and Potential Improvements

### Responsive layout testing

No tests currently render views at multiple widths. Text wrapping, truncation, and layout reflow are untested. A useful addition would be parameterised snapshot tests that render each view at several widths (e.g. 300, 500, 800pt) to catch clipping and overflow regressions.

### Interaction testing

The current tests are render-only — no user actions are simulated. Clicking a reaction, hovering over a timestamp, toggling a thread, or typing in the message input are all untested. This could be addressed with:

- XCTest UI tests against the running app for end-to-end interaction flows
- Unit tests against view model logic to verify state transitions without rendering

### Scroll and timeline behaviour

There are no tests for timeline scrolling, scroll anchoring when new messages arrive, or lazy loading of older messages. These are common sources of regressions (e.g. the timeline jumping when images load) and would benefit from integration-level tests.

### Dark mode and appearance variants

All snapshots are recorded in the default (light) appearance. There are no tests for dark mode, high contrast, or reduced transparency. Snapshot tests could be extended to render each view in multiple `colorScheme` environments.

### Accessibility

No tests verify Dynamic Type scaling, VoiceOver labels, or keyboard navigation. SwiftUI's `accessibilityLabel` and `accessibilityElement` modifiers are untested. The `AccessibilitySnapshot` library or manual accessibility audits could fill this gap.

### Edge case content

The mock data uses short, well-behaved strings. Untested scenarios include:

- Very long messages (thousands of characters, no whitespace)
- Right-to-left text (Arabic, Hebrew)
- Messages with many reactions (10+)
- Deeply nested thread replies
- Large numbers of read receipts
- Empty or missing display names / avatars
- Mixed inline content (links, code blocks, emoji-only messages)

### Animation and transition coverage

Snapshot tests capture a single frame and cannot verify animations, transitions, or timing. The hover-to-reveal timestamp in `GenericEventView`, reaction add/remove animations, and sheet presentation transitions are all untested.

### Test determinism

Mock data must avoid time-dependent values. `MockEventTimelineItem` previously used `.now` for its `date` property, causing snapshot tests to produce different timestamps on each run. All mocks should use fixed values — see `StableMockEvent` and the corrected `MockEventTimelineItem` for the pattern.

### Performance and stress testing

No tests measure rendering performance with large data sets (hundreds of timeline items, rooms with thousands of members). SwiftUI layout performance issues often only surface at scale and could be caught with targeted benchmarks using `XCTMetric`.
